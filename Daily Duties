#1 Make several blocks fall.

#2 Button that moves block in x-axis

#3 Remove artifact around blocks

Ask around about SPI.

At lunch hour, ask about "time.h" and "IOShieldOled.h" in relation to msys compile, and artefacts around falling blocks. 
Also if we should have our own method of drawing graphics.
