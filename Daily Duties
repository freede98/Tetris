#1 Button that moves block in x-axis

#2 Remove artifact around blocks

#3 Random() function

At lunch hour, ask about "time.h" in relation to msys compile, and artefacts around falling blocks. 
Also if we should have our own method of drawing graphics.
